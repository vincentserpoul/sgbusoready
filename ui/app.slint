import { Palette, Type } from "theme.slint";
import {
    PhoneColumn, AppBar, PrimaryButton, Chip, DayToggle, TextField, TimeStepper,
    FloatingAddButton, CommuteCard, StopResultRow,
} from "components.slint";
import { ScrollView } from "std-widgets.slint";

struct CommuteRow {
    label: string,
    status: string,
    active: bool,
    index: int,
}

struct StopResult {
    code: string,
    name: string,
    road: string,
}

enum Screen { list, editor, search }

export component AppWindow inherits Window {
    in property <[CommuteRow]> rows;
    in-out property <Screen> screen: Screen.list;
    // Top status-bar inset (the app draws edge-to-edge on Android).
    in property <length> top-inset: 0px;

    // Editor form state.
    in-out property <int> editing-index: -1;
    in-out property <string> form-stop-code;
    in-out property <string> form-stop-name;
    in-out property <string> form-line;
    in property <[string]> stop-services;
    in-out property <bool> day-mon;
    in-out property <bool> day-tue;
    in-out property <bool> day-wed;
    in-out property <bool> day-thu;
    in-out property <bool> day-fri;
    in-out property <bool> day-sat;
    in-out property <bool> day-sun;
    in-out property <int> start-hour: 8;
    in-out property <int> start-minute: 0;
    in-out property <int> end-hour: 9;
    in-out property <int> end-minute: 0;
    in-out property <string> error-text;

    // Stop-search state.
    in-out property <string> search-query;
    in property <[StopResult]> search-results;
    in property <bool> catalog-loading;

    callback save();
    callback delete(int);
    callback edit(int);
    callback new-commute();
    callback search-changed();
    callback stop-picked(string);
    callback pick-time(string);

    title: "SG Bus Ready";
    background: Palette.bg;
    preferred-width: 400px;
    preferred-height: 780px;

    // ---- List screen ----
    Rectangle {
        visible: root.screen == Screen.list;
        PhoneColumn {
        VerticalLayout {
            padding-left: 16px;
            padding-right: 16px;
            padding-bottom: 16px;
            padding-top: root.top-inset + 16px;
            spacing: 12px;
            Text {
                text: "Commutes";
                font-size: Type.title;
                font-weight: 800;
                color: Palette.text;
            }
            ScrollView {
                vertical-stretch: 1;
                VerticalLayout {
                    spacing: 10px;
                    alignment: start;
                    for row in root.rows: CommuteCard {
                        label: row.label;
                        status: row.status;
                        active: row.active;
                        clicked => {
                            root.edit(row.index);
                            root.screen = Screen.editor;
                        }
                    }
                    if root.rows.length == 0: Text {
                        text: "No commutes yet — tap + to add one.";
                        color: Palette.text-faint;
                        font-size: Type.body;
                    }
                }
            }
        }
        }
        FloatingAddButton {
            x: parent.width - self.width - 20px;
            y: parent.height - self.height - 36px;
            clicked => {
                root.new-commute();
                root.screen = Screen.editor;
            }
        }
    }

    // ---- Editor screen ----
    Rectangle {
        visible: root.screen == Screen.editor;
        PhoneColumn {
        VerticalLayout {
            padding-top: root.top-inset;
            AppBar {
                title: root.editing-index < 0 ? "New commute" : "Edit commute";
                show-back: true;
                action-text: root.editing-index < 0 ? "" : "Delete";
                back => { root.screen = Screen.list; }
                action => {
                    root.delete(root.editing-index);
                    root.screen = Screen.list;
                }
            }
            ScrollView {
                VerticalLayout {
                    padding: 16px;
                    spacing: 8px;
                    alignment: start;

                    Text { text: "Stop"; font-size: Type.label; color: Palette.text-faint; }
                    Rectangle {
                        height: 58px;
                        border-radius: 12px;
                        background: Palette.surface;
                        border-width: 1px;
                        border-color: Palette.hairline;
                        HorizontalLayout {
                            padding-left: 13px;
                            padding-right: 13px;
                            VerticalLayout {
                                horizontal-stretch: 1;
                                alignment: center;
                                spacing: 2px;
                                Text {
                                    text: root.form-stop-name == "" ? "Choose a stop" : root.form-stop-name;
                                    color: root.form-stop-name == "" ? Palette.text-faint : Palette.text;
                                    font-weight: 600;
                                    font-size: Type.body;
                                    overflow: elide;
                                }
                                if root.form-stop-code != "": Text {
                                    text: root.form-stop-code;
                                    font-size: Type.label;
                                    color: Palette.text-dim;
                                }
                            }
                            Text { text: "›"; font-size: 20px; color: Palette.text-dim; vertical-alignment: center; }
                        }
                        TouchArea {
                            clicked => {
                                root.search-query = "";
                                root.screen = Screen.search;
                            }
                        }
                    }

                    Text { text: "Line at this stop"; font-size: Type.label; color: Palette.text-faint; }
                    ScrollView {
                        height: 46px;
                        HorizontalLayout {
                            spacing: 8px;
                            for svc in root.stop-services: Chip {
                                text: svc;
                                selected: svc == root.form-line;
                                toggled => { root.form-line = svc; }
                            }
                            if root.stop-services.length == 0: Text {
                                text: "Pick a stop first";
                                color: Palette.text-faint;
                                font-size: Type.body;
                                vertical-alignment: center;
                            }
                        }
                    }

                    Text { text: "Active days"; font-size: Type.label; color: Palette.text-faint; }
                    HorizontalLayout {
                        spacing: 6px;
                        DayToggle { label: "M"; on: root.day-mon; toggled => { root.day-mon = !root.day-mon; } }
                        DayToggle { label: "T"; on: root.day-tue; toggled => { root.day-tue = !root.day-tue; } }
                        DayToggle { label: "W"; on: root.day-wed; toggled => { root.day-wed = !root.day-wed; } }
                        DayToggle { label: "T"; on: root.day-thu; toggled => { root.day-thu = !root.day-thu; } }
                        DayToggle { label: "F"; on: root.day-fri; toggled => { root.day-fri = !root.day-fri; } }
                        DayToggle { label: "S"; on: root.day-sat; toggled => { root.day-sat = !root.day-sat; } }
                        DayToggle { label: "S"; on: root.day-sun; toggled => { root.day-sun = !root.day-sun; } }
                    }

                    Text { text: "Window"; font-size: Type.label; color: Palette.text-faint; }
                    HorizontalLayout {
                        spacing: 10px;
                        TimeStepper {
                            caption: "Start";
                            hour: root.start-hour;
                            minute: root.start-minute;
                            picked => { root.pick-time("start"); }
                        }
                        TimeStepper {
                            caption: "End";
                            hour: root.end-hour;
                            minute: root.end-minute;
                            picked => { root.pick-time("end"); }
                        }
                    }

                    if root.error-text != "": Text {
                        text: root.error-text;
                        color: Palette.danger;
                        font-size: Type.caption;
                        wrap: word-wrap;
                    }

                    Rectangle { height: 6px; }
                    PrimaryButton {
                        text: "Save commute";
                        clicked => { root.save(); }
                    }
                }
            }
        }
        }
    }

    // ---- Stop-search screen ----
    Rectangle {
        visible: root.screen == Screen.search;
        PhoneColumn {
        VerticalLayout {
            padding-top: root.top-inset;
            AppBar {
                title: "Choose stop";
                show-back: true;
                back => {
                    sf.defocus();
                    root.screen = Screen.editor;
                }
            }
            VerticalLayout {
                padding: 16px;
                spacing: 10px;
                sf := TextField {
                    placeholder: "Search name or code";
                    text <=> root.search-query;
                    edited => { root.search-changed(); }
                }
                if root.catalog-loading: Text {
                    text: "Updating bus stops…";
                    color: Palette.text-faint;
                    font-size: Type.body;
                }
                ScrollView {
                    vertical-stretch: 1;
                    VerticalLayout {
                        spacing: 2px;
                        alignment: start;
                        for r in root.search-results: StopResultRow {
                            name: r.name;
                            sub: r.road + " · " + r.code;
                            clicked => {
                                sf.defocus();
                                root.stop-picked(r.code);
                                root.screen = Screen.editor;
                            }
                        }
                    }
                }
            }
        }
        }
    }
}
